今日计划：
1. 阅读参考论文得到第一类错误率的计算公式。<完成！>
	- 第一类错误率 = FN/(FN+TP)。第二类错误率 = FP/(FP+TN)。
	- 可以看出FN+TP是实际上所有的正样本数，recall = TN/(TN+FP)。因此第一类错误率 = 1-recall。
	- FP+TN是实际上所有的负样本数，第二类错误率无明显关系。
2. 修改print_analyse代码。计算第一类错误率和第二类错误率。<完成!>

3. 修改每个模型预测时的阈值，找一个使验证集上F1最大的阈值。然会重新计算每个模型的各个评价指标。<完成！>
	1. F1score的结果中，stacking的第一类错误率较大。不是很理想，而且stacking将recall和precision作为相同的权重。而我们更关心recall，所以现在采用f2 score.
	2. f2 = (1+4)(precision*recall)/(4*precision+recall)。

4. 使用stacking模型在验证集上的最优阈值，作为硬截断法的阈值。然后将拒绝样本加入训练，重新得到训练结果。<完成!>
	1. 由于拒绝推断的预测值不一样了，需要重新训练模型。但是最优的超参数不变。
	2. 将输出的结果打印，更新。

明天计划：
1. 整理代码，删除重复代码，对已有代码重构。
2. 阅读论文，提出论文修改意见。
